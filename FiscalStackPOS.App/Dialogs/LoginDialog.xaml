<Window x:Class="FiscalStackPOS.Dialogs.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource login.title}"
        Width="420" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}" FontFamily="Segoe UI"
        ShowInTaskbar="False">
    <Border Style="{StaticResource Card}" Margin="24">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Border Width="42" Height="42" CornerRadius="12" Background="{StaticResource PrimaryGradientBrush}"
                        VerticalAlignment="Center">
                    <TextBlock Text="FS" FontSize="16" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource login.title}" Style="{StaticResource H1Text}" />
                    <TextBlock Text="{DynamicResource login.subtitle}" Style="{StaticResource BodyText}" />
                </StackPanel>
            </StackPanel>

            <TextBlock Text="{DynamicResource login.username}" Style="{StaticResource CaptionText}" Margin="0,20,0,4" />
            <TextBox x:Name="TxtUsername" Style="{StaticResource FieldTextBox}" />
            <TextBlock Text="{DynamicResource login.password}" Style="{StaticResource CaptionText}" Margin="0,12,0,4" />
            <PasswordBox x:Name="TxtPassword" Style="{StaticResource FieldPasswordBox}" />
            <TextBlock x:Name="LblError" Foreground="{StaticResource DangerBrush}" FontSize="12"
                       Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="{DynamicResource common.exit}" Style="{StaticResource GhostButton}" IsCancel="True" MinWidth="76" />
                <Button Content="{DynamicResource login.signIn}" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" MinWidth="104"
                        Click="OnSignIn" IsDefault="True" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>